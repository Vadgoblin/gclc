mkdir -p ./dist ./.build-cache

docker run --rm \
     -v "$(pwd)/build-all.sh:/build-all.sh:ro" \
     -v "$(pwd)/..:/app-src:ro" \
     -v "$(pwd)/dist:/out" \
     -v "$(pwd)/.build-cache:/cache" \
     -e APP_VERSION="0.0.3" \
     -e HOST_UID="$(id -u)" \
     -e HOST_GID="$(id -g)" \
     ubuntu:22.04 /build-all.sh